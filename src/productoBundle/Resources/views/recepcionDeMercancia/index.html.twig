{% extends '::base.html.twig' %}

{% block body -%}
    <h1>recepcionDeMercancia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Producto</th>
                <th>Preciounitario</th>
                <th>Nodeproductos</th>
                <th>Preciototal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('recepciondemercancia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.producto }}</td>
                <td>{{ entity.precioUnitario }}</td>
                <td>{{ entity.noDeProductos }}</td>
                <td>{{ entity.precioTotal }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('recepciondemercancia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('recepciondemercancia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('recepciondemercancia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
