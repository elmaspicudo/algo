{% extends '::base.html.twig' %}

{% block body -%}
    <h1>recepcionDeMercancia</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Producto</th>
                <td>{{ entity.producto }}</td>
            </tr>
            <tr>
                <th>Preciounitario</th>
                <td>{{ entity.precioUnitario }}</td>
            </tr>
            <tr>
                <th>Nodeproductos</th>
                <td>{{ entity.noDeProductos }}</td>
            </tr>
            <tr>
                <th>Preciototal</th>
                <td>{{ entity.precioTotal }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('recepciondemercancia') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('recepciondemercancia_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
