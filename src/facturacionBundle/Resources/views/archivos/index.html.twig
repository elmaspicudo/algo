{% extends '::base.html.twig' %}

{% block body -%}
    <h1>archivos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cliente</th>
                <th>Tipo</th>
                <th>Nombre</th>
                <th>Contenido</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('archivos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cliente }}</td>
                <td>{{ entity.tipo }}</td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.contenido }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('archivos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('archivos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('archivos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
