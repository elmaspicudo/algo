{% extends '::base.html.twig' %}

{% block body -%}
    <h1>impuestos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Nombrecorto</th>
                <th>Porcentaje</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('impuestos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.nombreCorto }}</td>
                <td>{{ entity.porcentaje }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('impuestos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('impuestos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('impuestos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
