{% extends '::base.html.twig' %}

{% block body -%}
    <h1>polizas list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Version</th>
                <th>Rfc</th>
                <th>Mes</th>
                <th>Año</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('polizas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.version }}</td>
                <td>{{ entity.rfc }}</td>
                <td>{{ entity.mes }}</td>
                <td>{{ entity.año }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('polizas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('polizas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('polizas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
