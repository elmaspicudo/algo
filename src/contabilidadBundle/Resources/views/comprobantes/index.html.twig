{% extends '::base.html.twig' %}

{% block body -%}
    <h1>comprobantes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Uuid</th>
                <th>Monto</th>
                <th>Rfc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('comprobantes_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.uuId }}</td>
                <td>{{ entity.monto }}</td>
                <td>{{ entity.rfc }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('comprobantes_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('comprobantes_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('comprobantes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
