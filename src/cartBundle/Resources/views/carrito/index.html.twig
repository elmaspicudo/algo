{% extends '::base.html.twig' %}

{% block slider %}
    <h1>carrito list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Llave</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('carrito_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.llave }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('carrito_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('carrito_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('carrito_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
